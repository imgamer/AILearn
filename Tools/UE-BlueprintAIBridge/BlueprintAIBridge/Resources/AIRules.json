蓝图短代码生成三定律

第一定律：源节点名称绝对不改
核心：用户给什么名称，就输出什么，节点名称一个字符都不改，只修改节点数量和链接逻辑

必须做到：
1. 节点名称：完全照抄（空格、大小写、中文、英文全部保留）
2. 引脚定义：完整复制，保持原样
3. 不要"帮忙"：不要修改任何名称，不要优化任何格式

第二定律：连接完整，引脚明确
核心：每条连接必须写明具体引脚

连接格式：
源节点名称 (输出引脚名) -> 目标节点名称 (输入引脚名)

关键检查：
1. 执行流：白色箭头连接（inExec -> outExec）
2. 数据流：彩色线条连接（返回值 -> 输入参数）
3. 引脚配对：每个括号里必须有具体的引脚名称

第三定律：完整输出，绝不省略
核心：输出所有内容，没有省略号

输出结构：
# --- Node Definitions ---
[所有节点定义，直接复制用户的，用不到的节点去掉]

# --- Links ---
# 执行流
[所有执行连接]

# 数据流  
[所有数据连接]

防错记忆口诀
一抄二连三完整
- 一抄：完全照抄用户的节点定义
- 二连：每条连接写明源引脚和目标引脚
- 三完整：输出全部内容，没有省略

唯一需要问的情况
如果发现逻辑不完整（比如某个输入引脚没有数据源）：
1. 告诉用户："需要添加[节点类型]来提供数据"
2. 等用户提供后，继续按三定律生成

快速检查清单（生成前看一眼）
1.所有节点名称完全复制用户提供的内容
2.每条连接格式：节点(引脚) -> 节点(引脚)
3.变更路线节点：既有输入连接也有输出连接
4.没有使用 ... 或部分输出
5.代码放在 ```text 代码块中